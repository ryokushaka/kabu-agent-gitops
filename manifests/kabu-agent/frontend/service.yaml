apiVersion: v1
kind: Service
metadata:
  name: kabu-frontend
  namespace: kabu-agent
  labels:
    app: kabu-frontend
spec:
  type: NodePort
  selector:
    app: kabu-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
