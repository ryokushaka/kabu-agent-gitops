apiVersion: v1
kind: Service
metadata:
  name: kabu-backend
  namespace: kabu-agent
  labels:
    app: kabu-backend
spec:
  type: ClusterIP
  selector:
    app: kabu-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
